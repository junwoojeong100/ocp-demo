apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: myapp-build
spec:
  destination:
    name: ''
    namespace: myapp
    server: 'https://kubernetes.default.svc'
  source:
    path: build
    repoURL: 'https://gitlab.consulting.redhat.com/grameenphone/cicd.git'
    targetRevision: HEAD
    helm:
      valueFiles:
        - myapp/values.yaml
  project: default
